import Frame from "@/components/Frame";
import ComingSoon from "@/components/ComingSoon";

export default function PartidasPage() {
  return (
    <Frame variant="frame-dank-depths" title="Partidas">
      <ComingSoon
        phase="Fase 1 — Núcleo de Partidas"
        desc="Wizard de cadastro (setup do jogo → jogadores & personagens → estado final de cada player), listagem enxuta e tela de detalhe completa."
      />
    </Frame>
  );
}
